# Hack 1

Este proyecto consiste en replicar un layout utilizando HTML y CSS, respetando las reglas establecidas (sin float ni flex). 
Se utilizó `position: relative` y `absolute` para la disposición de los elementos.

Se coloco nombres a las clases 
